# python-code-for-a-Simple-ANN
The main idea is to show how the values move through the network for tanh activation function (inputs → hidden layer → output layer) without any training or backpropagation just pure forward computation.
